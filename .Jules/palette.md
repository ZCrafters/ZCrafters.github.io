# Palette's Journal

## 2025-02-26 - [Interactive Div Pattern]
**Learning:** This codebase uses `div` elements with `onclick` handlers for key interactions (Logo, Menu), which breaks keyboard accessibility.
**Action:** When identifying these patterns, refactor to semantic `<button>` or `<a>` elements while preserving existing classes to maintain visual fidelity.
