## 2024-05-22 - Semantic Navigation
**Learning:** Interactive `div` elements for primary navigation (like logos) break keyboard accessibility (Tab focus) and confuse screen readers.
**Action:** Always use `<button>` or `<a>` for interactive elements, even if they wrap images or complex content. Use `bg-transparent` and `border-none` to reset styles.
