## 2026-02-03 - Interactive Elements Semantics
**Learning:** Found legacy interactive elements implemented as `div` tags with `onclick` handlers (e.g., Logo) instead of semantic `<button>` or `<a>` tags.
**Action:** Refactor these to semantic HTML elements to ensure keyboard accessibility and proper screen reader support.
