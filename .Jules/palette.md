## 2026-01-24 - Semantic HTML for Interactive Elements
**Learning:** High-fidelity static sites often sacrifice semantic HTML (using `div` instead of `button`) for styling ease, which kills keyboard accessibility.
**Action:** Always verify interactive elements are `<button>` or `<a>` tags before applying ARIA labels.
